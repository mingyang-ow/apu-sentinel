# apu-sentinel — Scaffolding Brief for Claude Code

**Read this whole brief before doing anything. Your task in this first pass is
to create repository STRUCTURE and STUBS only — no modeling logic, no
algorithms, no training code. Directories, empty-but-structured files, the
filled-in `CLAUDE.md`, config skeletons, the model contract interface, the
Makefile, and test stubs. Nothing else. Do not implement any anomaly
detection, any data processing logic, or any model internals yet.**

Target location: `/home/bren/Projects/apu-sentinel` (this is a WSL2 machine).

---

## 0. Project in one line

Early-warning anomaly detection on the MetroPT-3 dataset (real Air Production
Unit / compressor telemetry from a metro train). Detect abnormal behaviour
early enough to be actionable, without excessive false alarms. This is an
**anomaly / early-warning** problem, NOT supervised failure classification —
there are too few real failure events to learn "failure vs not" without
overfitting.

---

## 1. Environment & tooling (set this up explicitly — do not choose your own)

- **Use `uv`** as THE environment and dependency manager. Not pip+venv, not
  Poetry, not conda.
- **Python 3.11** — pin it (`uv python pin 3.11`).
- `pyproject.toml` is the single source of dependency truth. Commit `uv.lock`.
- `make setup` runs `uv sync`.
- The project runs in TWO places from the same code: local WSL2 (CPU, small
  data subset, fast) and Google Colab (GPU/CUDA, full data, real runs).
  Behaviour differs by CONFIG only, never by editing code.

### PyTorch / CUDA — the one cross-environment seam (handle deliberately)

`uv.lock` makes ordinary dependencies identical across WSL2 and Colab, but
PyTorch is the exception: WSL2 (likely no GPU) needs a CPU torch wheel, Colab
needs the CUDA wheel for the T4. Configure `uv` with platform-specific torch
index sources in `pyproject.toml` so the correct wheel resolves in each
environment. Add a short comment in `pyproject.toml` explaining this so it is
not mistaken for a mistake later. Do NOT hard-pin a single torch build that
would break one of the two environments.

---

## 2. Repository structure (create exactly this; files are stubs)

```
apu-sentinel/
├── CLAUDE.md                    # fill in fully — content given in §3
├── README.md                    # short: what/why + quickstart, stub is fine
├── Makefile                     # targets given in §5
├── pyproject.toml               # uv, py3.11, torch index seam, ruff+pytest cfg
├── uv.lock                      # generated by uv
├── .pre-commit-config.yaml      # ruff lint+format; no-data-commit guard
├── .gitignore                   # must ignore data/ , mlruns/ , .venv/ etc.
│
├── .claude/
│   ├── hooks/                   # blocking leakage hook — see §4
│   └── commands/                # empty for now (placeholder .gitkeep)
│
├── configs/
│   ├── base.yaml                # shared truth — skeleton per §6
│   ├── local.yaml               # cpu, subset, fast, small max_minutes
│   ├── colab.yaml               # cuda, full data, real epochs, real budget
│   └── experiment/              # per-run overrides (.gitkeep for now)
│
├── data/                        # ALL gitignored
│   ├── raw/                     # MetroPT-3 as downloaded (.gitkeep)
│   ├── interim/                 # cleaned / regime-tagged (.gitkeep)
│   └── processed/               # model-ready windows (.gitkeep)
│
├── src/apu_sentinel/            # installable package (uv sync installs -e)
│   ├── __init__.py
│   ├── config.py                # pydantic-settings loader stub — see §6
│   ├── data/
│   │   ├── __init__.py
│   │   ├── download.py          # stub: fetch + checksum-verify (logic later)
│   │   ├── load.py              # stub
│   │   ├── split.py             # stub: TIME-BASED split ONLY (crown-jewel file)
│   │   └── windows.py           # stub: windowing + scaling, fit-on-train-only
│   ├── regimes/
│   │   └── __init__.py          # operating-state segmentation (later)
│   ├── features/
│   │   └── __init__.py
│   ├── models/
│   │   ├── __init__.py
│   │   ├── base.py              # THE CONTRACT — see §7, define interface only
│   │   ├── rule_based.py        # stub implementing the interface (pass)
│   │   ├── isolation_forest.py  # stub
│   │   └── autoencoder.py       # stub
│   ├── evaluation/
│   │   ├── __init__.py
│   │   ├── events.py            # stub: failure-window labels from doc'd dates
│   │   └── metrics.py           # stub: EPISODE-level metrics (see §3 metric)
│   ├── explain/
│   │   └── __init__.py          # per-channel attribution (CORE, not extension)
│   └── pipeline.py              # stub: ties stages, config-driven
│
├── scripts/                     # thin entry-points that call into the package
│   ├── bootstrap_colab.sh       # clone → make setup → ready (see §8)
│   ├── download.py              # thin wrapper over data/download.py
│   ├── train.py                 # thin; self-terminating on Colab (see §8)
│   ├── evaluate.py              # thin
│   └── baseline.py              # thin
│
├── tests/
│   ├── __init__.py
│   ├── conftest.py              # tiny synthetic fixture for smoke/contract
│   ├── test_split_no_leakage.py     # crown-jewel guard — see §4
│   ├── test_scaler_train_only.py    # crown-jewel guard — see §4
│   ├── test_eval_contract.py        # every model satisfies base.py
│   └── test_pipeline_smoke.py       # end-to-end on fixture, shape only
│
└── notebooks/
    └── exploratory/             # EDA ONLY, never source of truth (.gitkeep)
```

Notes: `src/apu_sentinel` is an **installable package** — this is the keystone:
tests import real modules (no `sys.path` hacks), and both WSL2 and Colab just
`uv sync`. Stubs should be importable (correct signatures, `pass` or
`raise NotImplementedError` bodies, docstrings stating intent) so the test
stubs and the smoke test can at least import without error.

---

## 3. CLAUDE.md — write this content in full

```markdown
# apu-sentinel

Early-warning anomaly detection on MetroPT-3, the Air Production Unit
(compressor) telemetry from a metro train. Real analog + digital sensor
signals over months, with only a handful of true failure events.

Goal: raise an alert early enough before a failure to be useful, without
excessive false alarms on normal operation. This is an **anomaly /
early-warning** problem, NOT supervised failure classification — too few
failures to learn "failure vs not" without overfitting.

## Architecture

Installable package `apu_sentinel` (`uv sync` installs it editable). The SAME
code runs locally (CPU, small subset) and on Colab (GPU, full data);
behaviour is selected by CONFIG, never by editing code. Env & deps managed by
uv; Python 3.11.

- data/       loading, TIME-BASED splitting, windowing + scaling
- regimes/    operating-state segmentation (compressor on/off cycles)
- features/   feature engineering
- models/     all models implement the models/base.py interface
- evaluation/ failure-window labels + EPISODE-LEVEL metrics
- explain/    per-channel reconstruction-error attribution (CORE)
- pipeline.py ties stages together, config-driven

## Hard rules — DO NOT violate

1. Split by time only. No random/shuffle split. No training sample's
   timestamp may exceed the train/val boundary. Enforced by
   tests/test_split_no_leakage.py (blocking hook on data/ edits).
2. Fit scalers on the TRAINING window ONLY. Never fit on val/test, never
   compute normalization stats over the full series. Enforced by
   tests/test_scaler_train_only.py (blocking hook).
3. No information flows backward in time. No future-derived features.
4. Condition on operating regime. The compressor cycles on/off; most raw
   variance is mode-switching, not anomaly. Models must account for state.
5. Evaluate at the EPISODE level, never per-timestamp, never per-parameter.
   See metric below.
6. Labels are derived, documented, versioned. Pre-failure windows come from
   documented failure dates; window width is SWEPT, not a magic number.

## The metric (what every model is scored on)

Detection and false alarms are counted at the EPISODE level.

- Alert = one episode. A contiguous run of abnormal behaviour is ONE alert,
  not one-per-timestamp and not one-per-spiking-channel. Episode boundaries
  use a documented hold-time / hysteresis rule (in config, not hardcoded).
- Each alert carries a MIXED, RANKED diagnosis: per-channel contributions for
  that episode (explain/), presented as one alert's detail — NOT as separate
  parallel alarms per channel.
- Detection (per failure event): did an alert episode fire within the
  pre-failure window, and lead time.
- False alarms: counted per false EPISODE on normal operation, each also
  carrying its mixed diagnosis — this is MATERIAL FOR ERROR ANALYSIS.

Primary objective: RECALL — catch the real events. False-alarm rate is a
MONITORED SECONDARY under a documented tolerable ceiling (set AFTER seeing
baseline behaviour, not before; starts null in config). Alerting always is
not a win. False episodes are studied, not merely penalised.

A model is "better" if it improves recall / lead time while keeping
false-alarms/day under the ceiling. Point accuracy and point F1 are NOT
headline metrics.

## Pre-failure window width

NOT a fixed number — a SWEPT hyperparameter explored on train/val ONLY (never
tuned against the few test failures — that is leakage). The detection-vs-
lead-time tradeoff curve is a REPORTED RESULT of the project. Too short →
warning too late to act on; too long → pre-failure label dilutes, detection
degrades.

## Model progression (each stage complete + beats the last before the next)

rule-based baseline → isolation forest → autoencoder / temporal model →
window-width sweep → per-signal explanation (CORE, feeds error analysis).
Streaming sim, alert API, dashboard, drift detection are scoped EXTENSIONS,
not core promises.

## Commands

- make setup               uv sync (install pkg + dev deps + pre-commit)
- make baseline            run rule-based baseline
- make train CONFIG=local  train on CPU/subset (default)
- make train CONFIG=colab  train on GPU/full data (self-terminating)
- make evaluate            episode-level evaluation
- make test                full suite (includes leakage guards)
- make lint                ruff

## Config

configs/base.yaml = shared. local.yaml = cpu/subset/fast. colab.yaml =
cuda/full/real-budget. Select with CONFIG=. Never hardcode paths,
hyperparameters, device, or runtime budget — all live in config.

## Do NOT

- Do not use a shuffle/random train-test split anywhere.
- Do not fit scalers or compute stats outside the training window.
- Do not add supervised failure classification as the primary model.
- Do not report point-wise accuracy/F1 as the headline result.
- Do not count alarms per-timestamp or per-parameter — episodes only.
- Do not hardcode paths/hyperparameters/device — put them in configs.
- Do not commit anything under data/.
```

---

## 4. Testing — three tiers, leakage guards BLOCKING

Tiers:
- **Pre-commit (Tier 1):** ruff lint + format; a guard that blocks committing
  anything under `data/`.
- **Claude Code hook (Tier 2):** on edits to `src/apu_sentinel/data/`, run the
  two leakage-guard tests. **Blocking** — if they fail, the edit must be
  resolved before proceeding. Configure this under `.claude/hooks/`. The hook
  runs exactly: `uv run pytest tests/test_split_no_leakage.py
  tests/test_scaler_train_only.py -q`. Keep the hook config minimal and
  human-readable so it can be reviewed before enabling.
- **pytest suite (Tier 3):** full `make test` — both leakage guards (again),
  the eval contract, the pipeline smoke test.

The two leakage guards live in BOTH Tier 2 and Tier 3 (same tests, two
triggers) — this is the intended belt-and-suspenders.

Blocking-hook note to bake in: write the guard tests to check FINAL state by
importing and exercising the functions, so a half-finished refactor fails
cleanly on import and gets fixed in-session, rather than tripping on transient
intermediate file states.

Test stubs to create (assertions described; leave bodies as
`pytest.skip("stub — implement with logic")` or minimal placeholders so the
suite runs green on a fresh scaffold):

- `test_split_no_leakage.py` — will assert
  `max(train.timestamp) < min(val.timestamp) < min(test.timestamp)`; and that
  the split function rejects any shuffle/random option.
- `test_scaler_train_only.py` — will fit a scaler through the real pipeline and
  assert its learned stats equal those from the training window alone and
  differ from full-series stats.
- `test_eval_contract.py` — will assert every model class implements the
  `models/base.py` interface, and that the evaluator returns expected
  episode-level detection on a planted synthetic failure.
- `test_pipeline_smoke.py` — will run the whole pipeline on the tiny fixture
  end-to-end and assert output shapes.

---

## 5. Makefile targets

`setup` (uv sync + install pre-commit hooks), `baseline`, `train` (takes
`CONFIG=`, default `local`), `evaluate`, `test`, `lint`. Targets should be
thin — call `uv run python scripts/...` or `uv run pytest`. `train` passes the
selected config through to `scripts/train.py`.

---

## 6. Config strategy (pydantic-settings, layered)

`config.py` defines a typed pydantic-settings schema (stub the classes with
fields + types, no loading logic beyond the skeleton). Loading model:
`base.yaml` merged with one env file (`local`/`colab`) chosen by `CONFIG`,
optional experiment overlay. Typed validation must fail loud at load time on
bad types/keys.

`base.yaml` skeleton must include (values as placeholders/comments):
```yaml
data:
  raw_dir: data/raw
  checksum: "<sha256 of MetroPT-3 — fill later>"
split:
  train_end: "<timestamp>"     # time-based boundary, NEVER a shuffle ratio
  val_end: "<timestamp>"
evaluation:
  window_widths: [ ]           # SWEPT list — a project result, not one number
  episode_hold_time: <seconds> # hysteresis for episode boundaries
  false_alarm_ceiling: null    # DEFERRED until baseline seen
model: { }                     # per-model hyperparameters
```
`local.yaml`: `device: cpu`, small `data.subset`, tiny `train.epochs`, small
`train.max_minutes`. `colab.yaml`: `device: cuda`, full `data.subset`, real
`train.epochs`, real `train.max_minutes` (the Colab budget — see §8).

The config structurally encodes the rules: split as timestamps (can't
shuffle), window widths as a list (must sweep), ceiling starts null (deferred).

---

## 7. Model contract — `models/base.py` (define interface only)

Every model implements the same interface; the evaluation layer does
everything else, so all models are scored identically:

```python
from typing import Protocol
import numpy as np

class AnomalyModel(Protocol):
    def fit(self, train_data) -> None: ...
    def score(self, data) -> np.ndarray:
        """Per-timestamp anomaly score."""
        ...
    def channel_contributions(self, data) -> np.ndarray:
        """Per-timestamp, per-channel contribution to the score.
        Feeds explain/ and the mixed ranked per-episode diagnosis."""
        ...
```

Models produce ONLY score + per-channel contribution. Thresholding, grouping
flagged timestamps into episodes (via hold-time), checking episodes against
pre-failure windows (detection + lead time), counting false episodes, and
attaching the ranked diagnosis are ALL done centrally in `evaluation/`. Models
never touch the metric. The three model stubs (`rule_based`,
`isolation_forest`, `autoencoder`) should declare the interface and
`raise NotImplementedError`.

---

## 8. Colab: same code, self-terminating runs, credit protection

`scripts/bootstrap_colab.sh`: clone the repo, `make setup` (uv sync), ready to
run — one command to reconstruct the ephemeral environment.

`scripts/train.py` on Colab must protect Colab Pro credits. Be honest about
limits in comments — code CANNOT guarantee Google fully reclaims the runtime
or stops billing on demand; it CAN stop doing compute and exit cleanly, and
prompt the user to confirm in the Colab UI. Implement three layers:

1. **Self-terminating run:** wrap training in `try/finally` — on completion OR
   exception, log the result, flush MLflow, and exit the process cleanly. No
   dangling live computation after the job.
2. **Wall-clock budget guard:** read `train.max_minutes` from config; abort the
   run if training exceeds it (protects against hangs / non-converging loops
   quietly eating GPU hours).
3. **Best-effort runtime disconnect:** at the very end, attempt Colab
   `runtime.unassign()` GUARDED in try/except (no-op / skip when not on Colab),
   AND print a clear message telling the user to verify in the Colab UI that
   the session actually ended — because the code cannot guarantee it.

`max_minutes` is a config field (small locally, real budget on Colab), fitting
the config strategy in §6.

---

## 9. Definition of done for THIS pass

- Full directory tree exists with `.gitkeep` where needed.
- `uv` initialised, Python 3.11 pinned, `pyproject.toml` with torch index seam
  commented, `uv.lock` generated, `uv sync` succeeds, package installs
  editable.
- `CLAUDE.md` filled in per §3.
- Makefile, configs skeleton, `config.py` schema stub, `models/base.py`
  contract, all package `__init__.py` and importable stubs present.
- `.claude/hooks/` blocking leakage hook configured (minimal, reviewable).
- `.pre-commit-config.yaml` (ruff + no-data-commit) present.
- Test stubs present; `make test` runs GREEN on the fresh scaffold (guards
  skipped/placeholder, smoke + contract pass trivially on stubs).
- `make lint` passes.
- NO modeling logic, NO algorithms, NO data-processing logic implemented.

When done, print a short tree of what was created and a one-line status for
each of the 5 locked decisions (structure / CLAUDE.md / tests / config /
contract) so it can be verified at a glance. Then STOP — do not begin
modeling.
```