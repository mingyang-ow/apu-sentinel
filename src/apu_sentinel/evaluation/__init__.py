"""Failure-window labels and EPISODE-LEVEL metrics.

All thresholding, episode grouping (hold-time/hysteresis), detection +
lead-time checks against pre-failure windows, false-episode counting, and
ranked diagnosis attachment live here -- never in models/ (CLAUDE.md rule 5).
"""
